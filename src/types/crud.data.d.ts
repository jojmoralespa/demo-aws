declare global {
    namespace CrudData {
        interface Product {
            productName: string;
            imageUrl: string;
            color: string;
            category: string;
            price: string;
        }
    }
}